//#include "get_next_line.h"
#include <unistd.h>
#include <stdlib.h>
#include <stdio.h>
#include <fcntl.h>

int main(int argc, char **argv)
{
	char *line;

	line = NULL;
	while (1);
	return (0);
//	while (get_next_line(0, &line) > 0)
//	{
//		printf("{%s}\n", line);
//		free(line);
//	}
//	printf("END of GNL");
//	return (0);
}
